Это проект поддержки процесса сборки для msbuild/xbuild.

Задача UpdateAssemblyInfo перед билдом при необходимости обновляет файл "Properties\AssemblyInfo.cs"
и заменяет в нём содержимое тегов данными из команды svn info
$WCURL$ - URL SVN-репозитория
$WCREV$ - ревизия SVN

Для использования этой задачи предполагается изменение *.csproj:

<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!-- Путь до сборки должен быть верным  -->
  <UsingTask TaskName="ProSoft.MsBuild.UpdateAssemblyInfo" AssemblyFile="..\msbuild\bin\Release\ProSoft.MsBuild.dll" />

...


  <ItemGroup>
...
	<!-- Добавили не существующий файл, который будет сгенерирован на BeforeBuild. -->
    <Compile Include="Properties\AssemblyInfo.cs" />
...
  </ItemGroup>

...
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
  <Target Name="BeforeBuild">
    <UpdateAssemblyInfo TemplateFile='Properties\AssemblyInfo.cs.in' OutputFile='Properties\AssemblyInfo.cs' />
  </Target>
... 


Для удобства работы с этой задачей предлагается добавить в файл ".svnignore" файл AssemblyInfo.cs
